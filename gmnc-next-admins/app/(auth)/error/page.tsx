export default function AuthErrorRoute() {
  return (
    <div className="flex min-h-screen items-center justify-center bg-slate-50 p-6 text-center text-sm text-slate-500">
      Auth error page is temporarily disabled to keep the build working.
    </div>
  );
}
